# Keep Pi Web subtree source unmodified for desktop branding

Pi Web is imported as an upstream git subtree that should remain easy to update. We will apply the `Pi Agent App` branding from the desktop wrapper layer rather than directly editing Pi Web source, accepting a small amount of wrapper-side override complexity to reduce future subtree update conflicts.
